{% extends "base.jinja2" %}

{% block content %}
    <h2>Изменение профиля xunto</h2>

    <div id="quentaSetting_">
        <p>
            <a href="{{ url_for("custom_pages.custom_page", identifier="setting") }}">
                Перед написанием квенты, прочитайте краткий background</a>
        </p>
        <p>
            Также прочитайте о <a href="{{ url_for("custom_pages.custom_page", identifier="dice") }}">системе</a>.
            Список навыков возьмите <a href="/static/skills">отсюда сразу соформлением</a> и скопируйте в конец квенты,
            расставив значения.
        </p>
    </div>
    <h3>И заполните поля</h3>
    <form class="editprofile" method="POST">
        <table>
            {% for field in form %}
                <tr>
                    <th>{{ field.label }}</th>
                    <td>
                        {{ field }}
                    </td>
                </tr>
            {% endfor %}
        </table>
        <input type="submit" name="submit" value="Применить"/>
    </form>
{% endblock %}